package {{package}}
import io.vertx.core.Vertx
import io.vertx.core.AbstractVertical
import io.vertx.serviceproxy.ServiceBinder

fun main(){
    Vertx.vertx().deployVertical({{classname}}Vertical())
}

class {{classname}}Vertical:AbstractVertical() {

    override fun start() {
        val instance = (javaClass.classLoader.loadClass("{{package}}.{{classname}}Impl").newInstance() as {{classname}})
        instance.init(vertx,config())
        ServiceBinder(vertx)
            .setAddress({{classname}}.address)
            .register({{classname}}::class.java,instance)
    }
}